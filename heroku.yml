setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile.web
  config:
    DJANGO_ALLOWED_HOSTS: "*"
    DJANGO_CORS_ORIGIN_WHITELIST: "https://aec.works"
    SERVE_STATIC: 1
